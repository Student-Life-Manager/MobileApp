export enum OutpassStatus {
	Approved = 'Approved',
	Pending = 'Pending',
	Rejected = 'Rejected',
	Returned = 'Returned',
}

export enum KeyboardType {
	NUMERIC = 'numeric',
	LETTER = 'default',
	EMAIL = 'email-address',
}
