export enum OutpassStatus {
	Approved = 'in_campus',
	Pending = 'created',
	Rejected = 'rejected',
	Returned = 'returned_to_campus',
	Exited = 'exited_campus',
}

export enum KeyboardType {
	NUMERIC = 'numeric',
	LETTER = 'default',
	EMAIL = 'email-address',
}
